import 'package:flutter/material.dart';

class AppColors {
  // Colores Base
  static const Color primaryGreen = Color(0xFF6FB634);
  static const Color darkBlue = Color(0xFF0E1428);
  static const Color bgColor = darkBlue;

  // Variantes Dark para Conductores
  static const Color surfaceDark = Color(
    0xFF1C2237,
  ); // Un azul un poco más claro para tarjetas
  static const Color accentGreen = Color(
    0xFF86EFAC,
  ); // Verde neón para estados activos

  // Soporte
  static const Color white = Color(0xFFFFFFFF);
  static const Color greyLight = Color(0xFFF5F5F5);
  static const Color greyText = Color(
    0xFF94A3B8,
  ); // Gris azulado para textos secundarios

  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
}
